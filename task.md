# Get one field value by name

Looking at file `reflection.go`.
We currently have a function named `GetFieldValuesByNames`.
Please add a new function: `GetFieldValuesByName`, accepting only one field name.
Use the existing function as an example.
Function `GetFieldValuesByNames` should call `GetFieldValuesByName` internally for every field in the array.
Good luck.
